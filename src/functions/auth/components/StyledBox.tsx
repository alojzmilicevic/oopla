import { Box, styled } from "@mui/material";

export const StyledBox = styled(Box)({
    position: "absolute",
    top: 0,
    left: 0,
    width: "100vw",
    height: "100vh",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
});